# ---------------------------------------------------------------------------
# Server requirements - GPU Pod (H100 80GB) - Python 3.11 + CUDA 12.x
# Install:  python3.11 -m pip install -r server/requirements.txt
# ---------------------------------------------------------------------------

# --- Web / streaming ---
fastapi==0.115.2
uvicorn[standard]==0.31.1
websockets==13.1

# --- DSP / array math ---
numpy==1.26.4
soxr==0.5.0.post1
scipy==1.14.1
soundfile==0.12.1

# --- Torch stack (install the CUDA 12.1 build on the pod) ---
torch==2.4.1
torchaudio==2.4.1

# --- 2. Deep Noise Filter: YAMNet ---
tensorflow==2.17.0
tensorflow-hub==0.16.1

# --- 3. Overlap Resolver (DSP) ---
pedalboard==0.9.16

# --- 4. Speaker Diarization / voiceprint ---
pyannote.audio==3.3.2

# --- 5. Language ID ---
speechbrain==1.0.1

# --- 6. ASR ---
faster-whisper==1.0.3

# --- 7. Translation LLM ---
vllm==0.6.3

# --- Utilities ---
python-dotenv==1.0.1
rich==13.9.2
