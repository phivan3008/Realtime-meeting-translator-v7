# ---------------------------------------------------------------------------
# Client requirements - Windows Client PC - Python 3.11
# Install:  py -3.11 -m pip install -r client/requirements.txt
# ---------------------------------------------------------------------------

# --- Audio capture (WASAPI loopback) ---
PyAudioWPatch==0.2.12.7

# --- DSP / array math ---
numpy==1.26.4
soxr==0.5.0.post1          # High quality streaming resampler (48k -> 16k)
scipy==1.14.1              # Fallback resampler + WAV helpers

# --- Voice Activity Detection (CPU) ---
torch==2.4.1               # CPU wheel is enough on the client
silero-vad==5.1.2
onnxruntime==1.19.2        # Optional ONNX backend for Silero VAD

# --- Networking ---
websockets==13.1

# --- UI ---
PySide6==6.7.3

# --- Utilities ---
python-dotenv==1.0.1
rich==13.9.2               # Console logging / test reports
